# GP-API Credentials (from developer.globalpay.com)
GP_APP_ID=your_app_id_here
GP_APP_KEY=your_app_key_here

# Environment: sandbox or production
GP_ENVIRONMENT=sandbox

# Merchant ID from the developer portal
GP_MERCHANT_ID=MER_your_merchant_id_here

# Account ID — TRA_ prefixed value for your channel
GP_ACCOUNT_ID=TRA_your_account_id_here

# Account name — must match portal EXACTLY (case-sensitive)
GP_ACCOUNT_NAME=transaction_processing

# 3DS2 Notification URLs
# Both URLs must be publicly reachable — the ACS (bank) server POSTs to them.
# This app serves both endpoints locally at /3ds/method-notification and /3ds/challenge-notification.
# For local development expose them via ngrok: ngrok http 8000
# Then set both vars to your ngrok public URL + the path below.
METHOD_NOTIFICATION_URL=https://<your-public-host>/3ds/method-notification
CHALLENGE_NOTIFICATION_URL=https://<your-public-host>/3ds/challenge-notification
